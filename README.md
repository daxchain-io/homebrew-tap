# Daxchain Homebrew Tap

This repository is the shared Homebrew tap for Daxchain command-line tools.
Formulae and casks are generated by each upstream project's release workflow.

```sh
brew update
brew install --cask daxchain-io/tap/daxie
```
